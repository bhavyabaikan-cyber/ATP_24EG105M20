# Week 5 Assignments 

## 🎯 Learning Goals
- [ ] Build structured tables using `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>`
- [ ] Master `colspan` and `rowspan` for complex cell layouts
- [ ] Style tables with CSS (`border-collapse`, `padding`, `text-align`, `margin`)
- [ ] Link external CSS correctly to HTML

##  Files Included
| File | Purpose |
|------|---------|
| `01-basic-colspan-table.html` + `.css` | Simple table with `colspan` headers & subtitles |
| `02-grades-rowspan-table.html` + `.css` | Student grades table using `rowspan` & `colspan` |

## ▶️ How to View
Open directly in any browser:

# Windows
start 01-basic-colspan-table.html
start 02-grades-rowspan-table.html


## 🎯 Learning Goals
- [ ] Build structured tables using `<thead>`, `<tbody>`, `colspan` & `rowspan`
- [ ] Create modern UI layouts using CSS Flexbox (`display: flex`, `justify-content`, `gap`)
- [ ] Style reusable card components with shadows, borders, and rounded images
- [ ] Link external CSS correctly and manage static assets (`./assets/`)

##  Files Included
| File | Purpose |
|------|---------|
| `01-basic-colspan-table.html` + `.css` | Simple table with `colspan` headers & subtitles |
| `02-grades-rowspan-table.html` + `.css` | Student grades table using `rowspan` & `colspan` |
| `03-travel-cards-layout.html` + `.css` | Flexbox navbar + image card grid with box shadows |

## ▶️ How to View
Open directly in any browser:
```bash
start 01-basic-colspan-table.html
start 02-grades-rowspan-table.html
start 03-travel-cards-layout.html